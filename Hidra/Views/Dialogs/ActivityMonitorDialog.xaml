<UserControl x:Class="Hidra.Views.Dialogs.ActivityMonitorDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Hidra.Views.Dialogs"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             d:DesignHeight="360" d:DesignWidth="480">
    <Grid Margin="16" Width="480">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="200" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Margin="4" Text="Remap activity" Style="{DynamicResource MaterialDesignHeadline6TextBlock}"/>
        <TextBlock Grid.Row="1" Margin="4 0 4 8" Text="Remap events fired per second, across every mapping in the active profile."
                   Style="{DynamicResource MaterialDesignBody2TextBlock}" Opacity="0.7"/>

        <Border Grid.Row="2" Margin="4" Background="{DynamicResource MaterialDesignBackground}"
                BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="1">
            <Grid>
                <Canvas x:Name="GraphCanvas" ClipToBounds="True" SizeChanged="GraphCanvas_OnSizeChanged">
                    <Polyline x:Name="GraphLine" Stroke="{DynamicResource PrimaryHueMidBrush}" StrokeThickness="2"/>
                </Canvas>
                <StackPanel HorizontalAlignment="Right" VerticalAlignment="Top" Margin="8" Orientation="Vertical">
                    <TextBlock Text="{Binding CurrentRate, StringFormat={}{0:0} events/s}"
                               Style="{DynamicResource MaterialDesignBody1TextBlock}"/>
                    <TextBlock Text="{Binding PeakRate, StringFormat=Peak: {0:0}/s}"
                               Style="{DynamicResource MaterialDesignCaptionTextBlock}" Opacity="0.7"/>
                </StackPanel>
            </Grid>
        </Border>

        <StackPanel Grid.Row="3" HorizontalAlignment="Right" Orientation="Horizontal" Margin="0 12 0 0">
            <Button IsDefault="True" IsCancel="True"
                    Style="{DynamicResource MaterialDesignFlatButton}"
                    Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                    Content="CLOSE">
                <Button.CommandParameter>
                    <system:Boolean>True</system:Boolean>
                </Button.CommandParameter>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
