**What this changes and why**

**How you tested it**

- [ ] `dotnet build Hidra.sln` and `dotnet test Hidra.sln` both pass
- [ ] If this touches `Core_RawInputHook.cs`, `SingleGlobalInstance.cs`, or `App.xaml.cs`'s
      `UnblockFiles()`, I've read `SECURITY.md` first
